This is Perl module Font::Subset.

INSTALLATION

Type the following command:

    $ curl -L http://cpanmin.us | perl - Font::Subset

Or install cpanm and then run the following command to install
Font::Subset:

    $ cpanm Font::Subset

If you get an archive of this distribution, unpack it and build it
as per the usual:

    $ tar xzf Font-Subset-$version.tar.gz
    $ cd Font-Subset-$version
    $ perl Makefile.PL
    $ make && make test

Then install it:

    $ make install

DOCUMENTATION

Font::Subset documentation is available as in POD. So you can do:

    $ perldoc Font::Subset

to read the documentation online with your favorite pager.

LICENSE AND COPYRIGHT

Copyright (c) 2012, Kenta Sato. All rights reserved.

This library is free software; you can redistribute it and/or modify
it under the same terms as Perl itself.
